"""Pytest configuration for Unlimited-OCR tests."""

import sys
import os

# Ensure the repo root is on sys.path so `import infer` works
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
